	{% extends "layout.html.twig" %}
	
	{% block title %}Inscription{% endblock %}
	
	{% block content %}
            <h2>Formulaire d'inscription</h2>

            <p>Veillez remplir le formulaire ci-dessous pour vous inscrire. Vous recevrez une confirmation par email une fois que le formulaire sera validé.</p>
            <p>Les champs marqués d'une astérisque sont obligatoires</p>
            
            <div>
            {{ form_start(userForm, { 'attr': {'class': 'form-horizontal'} }) }}
                <div class="form-group">
                    {{ form_label(contactForm.username, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.username) }}
	                    </div>
                        {{ form_widget(userForm.username, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.firstname, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.firstname) }}
	                    </div>
                        {{ form_widget(userForm.firstname, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.email, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.email) }}
	                    </div>
                        {{ form_widget(userForm.email, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.password, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.password) }}
	                    </div>
                        {{ form_widget(userForm.password, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(userForm.phone, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.phone) }}
	                    </div>
                        {{ form_widget(userForm.phone, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>     
                <div class="form-group">
                    {{ form_label(userForm.city, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.city) }}
	                    </div>
                        {{ form_widget(userForm.city, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>  
                <div class="form-group">
                    {{ form_label(userForm.role, null) }}
                    <div>
	                    <div class="alert alert-danger">
	                        {{ form_errors(userForm.role) }}
	                    </div>
                        {{ form_widget(userForm.role, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>   
                                                                 
                <div class="form-group">
                    <input type="submit" class="btn btn-primary btn-lg" value="send" />
                </div>
            {{ form_end(userForm) }}
        </div>

		{% endblock %}
		