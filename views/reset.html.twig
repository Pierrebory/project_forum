    {% extends "layout.html.twig" %}
    
    {% block title %}Inscription{% endblock %}
    
    {% block content %}
            <h2 style="margin-left: 18.5em; margin-right: 20em; margin-top: 1em;">Mot de passe oublié</h2>

            <p style="margin-left: 40em; margin-right: 40em">Vous avez oublié votre mot de passe ? Pas de panique, entrez votre adresse email pour reçevoir un message vous permettant de modifier votre mot de passe.</p>

            {% for flashMessage in app.session.flashbag.get('success') %}
            <div class="alert alert-success">
                {{ flashMessage }}
            </div>
            {% endfor %}    

            {% for flashMessage in app.session.flashbag.get('error') %}
            <div class="alert alert-danger" >
                {{ flashMessage }}
            </div>
            {% endfor %}            


            <div>
            {{ form_start(resetForm, { 'attr': {'class': 'form-horizontal'} }) }}

                <div class="form-group" style="margin-left: 40em; margin-right: 40em">
                    {{ form_label(resetForm.email, null) }}
                    <div>
                        {% if resetForm.email.vars.errors|length > 0 %}
                            <div class="alert alert-danger">
                                {% for error in resetForm.email.vars.errors %}
                                    {{ error.message }}
                                {% endfor %}
                            </div>
                        {% endif %}
                        {{ form_widget(resetForm.email, { 'attr':  {
                            'class': 'form-control'
                        }}) }}
                    </div>
                </div>

                <div class="form-group" style="margin-left: 60em; margin-right: 60em; margin-top: 2em;">
                    <input type="submit" class="btn btn-primary btn-lg" value="Envoyer" />
                </div>


            {{ form_end(resetForm) }}
            
        </div>

        {% endblock %}
        